# Agent Instructions

Before working in this repository, read all Markdown files in `dev/agents/`
in ascending filename order.

Those files contain the repository-wide agent instructions unless a file
explicitly limits its own scope.
